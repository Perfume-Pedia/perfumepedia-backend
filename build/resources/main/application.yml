spring:
  datasource:
    url: jdbc:mysql://15.164.129.164:3306/PerfumePedia?serverTimezone=UTC&characterEncoding=UTF-8
    username: PERFUME
    password: rnjswodn1!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
